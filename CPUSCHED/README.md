
# CPU Scheduler Overview

## How to compile:
Run this command in the terminal: `g++ main.cpp scheduler.cpp -o sched`

## Run executable:
Run this command: `./sched`
 * Choose Option 1 to run the FIFO scheduling algorithm.
 * Choose Option 2 to run the SJF scheduling algorithm.

## Input
`input.txt` (arrival burst per line)